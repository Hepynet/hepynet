config:
    include:
        - "share/lfv/train/emu_pnn.yaml"

job:
    job_name: "emu-s1_b20"
    job_type: "train"
    save_dir: "run/lfv/emu"

input:
    bkg_sumofweight: 20000
    sig_sumofweight: 1000

train:
    sig_class_weight: 1.
    bkg_class_weight: 1.